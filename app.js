const internalMessage = `This is a working process of learning GIT, the connections therein and its relation to GitHub`;

console.log(internalMessage);

const internalMessage_2 = `Adapted through Manuel Pinto's CodePen, through Colt Steele's Youtube Channel`;

console.log(internalMessage_2);